# 1. How to find the missing number in a given integer array of 1 to 100?

def missingnum(my_list):
    lenlist = len(my_list)
    print(lenlist)
    sumOfN = ((lenlist+1)*(lenlist+2))/2
    totalcount = 0
    for ele in my_list:
        totalcount = totalcount + ele
    print(sumOfN)
    print(totalcount)
    return print(sumOfN-totalcount)    

my_list = [1,2,3,4,5,6,8,9,10]
missingnum(my_list)
